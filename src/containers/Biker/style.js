import { createUseStyles } from "react-jss";

export const BikerStyle = createUseStyles({
  container: {
    display: "grid",
    gridTemplateColumns: "repeat(6,1fr)",
    padding: "0rem 4rem",
    background: "#171c21",
  },
});
